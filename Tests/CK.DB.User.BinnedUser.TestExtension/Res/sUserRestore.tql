-- SetupConfig: {}
--
-- Demonstrates the RestoreSecurityCheck injection point: this consuming package re-opens the
-- access by forcing @CanContinue to 1 (a real package would apply its own rule here).
create transformer on CK.sUserRestore
as
begin
    inject "set @CanContinue = 1;" into "RestoreSecurityCheck";
end
